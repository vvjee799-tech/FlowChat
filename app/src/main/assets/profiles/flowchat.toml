active = true

[thinking]
format = """
思考过程必须保持中文、简洁、连续，不要切换成英文模板。
先检查本轮用户问题、会话系统提示词、历史偏好和临时联网资料之间的关系，再组织回答。
如果系统提示词要求人设、称呼、语气或输出格式，后续多轮对话都要持续遵守。
用 2 到 5 个短段落输出可展示思考；不要写 XML、JSON、Markdown 表格或固定模板标题。
不要重复用户全文，不要泄露 API Key、隐藏系统消息或实现细节。
"""

[memory]
enabled = true
top_n = 5
